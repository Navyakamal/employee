const { errorLogger } = require("./employeeLogger/errorLogger");
const { performanceLogger } = require("./employeeLogger/performanceLogger");

module.exports={
    errorLogger,
    performanceLogger
}